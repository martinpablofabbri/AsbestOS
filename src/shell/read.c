#include <stdio.h>
#include <stdlib.h>

#include "command.h"

Command_vec* read_commands() {
  // TODO(jg): remove
  printf("In read_commands()\n");

  Command_vec* cv = NULL;
  // Set up cv
  return cv;
}
